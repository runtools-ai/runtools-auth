# ==============================================================================
# runtools-auth — Unified Authentication & Credential Management
# ==============================================================================

# ── Server ────────────────────────────────────────────────────────────────────
HOST=0.0.0.0
PORT=8420
BASE_URL=http://localhost:8420

# ── Database (PostgreSQL — shared with orchestrator/tools/billing) ────────────
DATABASE_URL=postgres://user:password@localhost:5432/runtools

# ── Crypto ────────────────────────────────────────────────────────────────────
# 32-byte base64-encoded keys. Generate with: openssl rand -base64 32
MASTER_KEY=your-32-byte-base64-master-key
HMAC_SECRET=your-32-byte-base64-hmac-secret

# ── Service-to-service auth ──────────────────────────────────────────────────
# Single shared secret replacing FC_SECRET / INTERNAL_SECRET / BILLING_INTERNAL_SECRET
AUTH_SERVICE_SECRET=your-shared-service-secret

# ── WorkOS ────────────────────────────────────────────────────────────────────
WORKOS_API_KEY=sk_live_xxx
WORKOS_CLIENT_ID=client_xxx
WORKOS_COOKIE_PASSWORD=your-32-char-cookie-password
# Webhook signing secret — find in WorkOS Dashboard > Webhooks
WORKOS_WEBHOOK_SECRET=your-workos-webhook-secret
# WORKOS_JWKS_URL=https://api.workos.com/sso/jwks/client_xxx

# ── OAuth Providers (all optional — only registered if both id+secret present) ─
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
X_CLIENT_ID=
X_CLIENT_SECRET=
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
TELEGRAM_BOT_TOKEN=
WHATSAPP_CLIENT_ID=
WHATSAPP_CLIENT_SECRET=

# ── Logging ───────────────────────────────────────────────────────────────────
RUST_LOG=runtools_auth=info
